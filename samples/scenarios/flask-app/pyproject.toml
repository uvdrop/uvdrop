[project]
name = "uvdrop-sample-flask-app"
version = "0.0.1"
description = "create Flask app object, no server"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
]

[tool.uv]
package = false
