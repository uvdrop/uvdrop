[project]
name = "uvdrop-sample-pyautogui-import"
version = "0.0.1"
description = "import only (no input injection)"
requires-python = ">=3.11"
dependencies = [
    "pyautogui>=0.9",
]

[tool.uv]
package = false
