[project]
name = "uvdrop-sample-transformers-tiny"
version = "0.0.1"
description = "import transformers (no model download)"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.40",
    "torch",
]

[tool.uv]
package = false
