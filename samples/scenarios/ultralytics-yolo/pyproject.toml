[project]
name = "uvdrop-sample-ultralytics-yolo"
version = "0.0.1"
description = "import ultralytics (no weights download)"
requires-python = ">=3.11"
dependencies = [
    "ultralytics",
]

[tool.uv]
package = false
