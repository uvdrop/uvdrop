[project]
name = "uvdrop-sample-faiss-cpu"
version = "0.0.1"
description = "tiny index add/search"
requires-python = ">=3.11"
dependencies = [
    "faiss-cpu",
    "numpy>=1.26",
]

[tool.uv]
package = false
