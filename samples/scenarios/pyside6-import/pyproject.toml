[project]
name = "uvdrop-sample-pyside6-import"
version = "0.0.1"
description = "Qt binding smoke (no window)"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.6",
]

[tool.uv]
package = false
