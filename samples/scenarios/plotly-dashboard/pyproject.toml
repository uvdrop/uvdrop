[project]
name = "uvdrop-sample-plotly-dashboard"
version = "0.0.1"
description = "plotly figure build (no browser)"
requires-python = ">=3.11"
dependencies = [
    "plotly>=5.18",
    "pandas>=2.0",
]

[tool.uv]
package = false
