[project]
name = "uvdrop-sample-timeseries-statsmodels"
version = "0.0.1"
description = "statsmodels import + tiny ARIMA-ready series"
requires-python = ">=3.11"
dependencies = [
    "statsmodels>=0.14",
    "pandas>=2.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
