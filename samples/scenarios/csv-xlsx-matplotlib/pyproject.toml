[project]
name = "uvdrop-sample-csv-xlsx-matplotlib"
version = "0.0.1"
description = "pandas + openpyxl + matplotlib (Agg)"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "openpyxl>=3.1",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
