[project]
name = "uvdrop-sample-streamlit-import"
version = "0.0.1"
description = "import streamlit (no server)"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.30",
]

[tool.uv]
package = false
