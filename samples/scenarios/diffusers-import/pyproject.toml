[project]
name = "uvdrop-sample-diffusers-import"
version = "0.0.1"
description = "import diffusers (no pipeline download)"
requires-python = ">=3.11"
dependencies = [
    "diffusers>=0.27",
    "torch",
]

[tool.uv]
package = false
