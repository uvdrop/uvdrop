[project]
name = "uvdrop-sample-fastapi-app"
version = "0.0.1"
description = "create FastAPI app object, no server"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
]

[tool.uv]
package = false
