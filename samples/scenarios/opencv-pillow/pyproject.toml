[project]
name = "uvdrop-sample-opencv-pillow"
version = "0.0.1"
description = "image array smoke"
requires-python = ">=3.11"
dependencies = [
    "opencv-python-headless>=4.8",
    "Pillow>=10.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
