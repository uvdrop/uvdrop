[project]
name = "uvdrop-sample-audio-librosa"
version = "0.0.1"
description = "librosa import"
requires-python = ">=3.11"
dependencies = [
    "librosa>=0.10",
    "numpy>=1.26",
]

[tool.uv]
package = false
