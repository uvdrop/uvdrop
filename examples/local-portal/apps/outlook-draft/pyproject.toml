[project]
name = "uvdrop-portal-outlook-draft"
version = "0.0.1"
description = "Create Outlook mail drafts via COM with a polished UI"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
    "pywin32>=306",
]

[tool.uv]
package = false
