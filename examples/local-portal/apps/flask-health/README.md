# Flask health

小さな Flask サーバを起動し、ブラウザで確認するポータル用サンプルです。

## 使い方

1. uvdrop から起動する  
2. 制御ウィンドウが開き、`http://127.0.0.1:8760/` を表示  
3. 「ブラウザで開く」または自動でブラウザが開く  
4. `/health` で JSON `{"status":"ok",...}`  
5. 制御ウィンドウを閉じるとサーバ停止

以前の版は import スモークだけで即終了していました。いまは常駐します。
