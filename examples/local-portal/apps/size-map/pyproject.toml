[project]
name = "uvdrop-portal-size-map"
version = "0.0.1"
description = "Folder size treemap — find fat folders visually"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
]

[tool.uv]
package = false
