[project]
name = "uvdrop-portal-rename-preview"
version = "0.0.1"
description = "Batch rename with large before/after preview"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
]

[tool.uv]
package = false
