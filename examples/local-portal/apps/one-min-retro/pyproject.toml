[project]
name = "uvdrop-portal-one-min-retro"
version = "0.0.1"
description = "One-minute Keep/Problem/Try retro board (local, visual)"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
    "pillow>=10.0",
]

[tool.uv]
package = false
