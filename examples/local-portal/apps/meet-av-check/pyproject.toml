[project]
name = "uvdrop-portal-meet-av-check"
version = "0.0.1"
description = "Tkinter camera and microphone check before remote meetings"
requires-python = ">=3.11"
dependencies = [
    "opencv-python-headless>=4.8",
    "sounddevice>=0.4",
    "numpy>=1.24",
    "pillow>=10.0",
]

[tool.uv]
package = false
