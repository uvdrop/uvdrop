[project]
name = "uvdrop-portal-clip-factory"
version = "0.0.1"
description = "Clipboard image trim / resize / copy-back factory"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
    "pillow>=10.0",
]

[tool.uv]
package = false
