[project]
name = "uvdrop-portal-ocr-bench"
version = "0.0.2"
description = "OCR bench with pip-only engines (RapidOCR + EasyOCR)"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10.0",
    "opencv-python-headless>=4.8",
    "numpy>=1.24",
    "rapidocr-onnxruntime>=1.3",
    "easyocr>=1.7",
]

[tool.uv]
package = false
