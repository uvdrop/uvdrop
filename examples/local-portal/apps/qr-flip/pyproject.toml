[project]
name = "uvdrop-portal-qr-flip"
version = "0.0.1"
description = "Flip-book QR transfer between nearby PCs (air-gap demo)"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.5",
    "pillow>=10.0",
    "qrcode>=7.4",
    "opencv-python-headless>=4.8",
    "numpy>=1.24",
]

[tool.uv]
package = false
